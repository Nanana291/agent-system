# Claude Change Memory

Change-specific lessons captured from Claude runs live here.

- Keep prompts narrow when the change is only about orchestration.
- Keep host-local lessons inside Claude's own memory file.
