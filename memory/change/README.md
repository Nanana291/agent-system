# Change Memory

This layer stores lessons captured from `change gate` for the active host.

## Policy

- Use it for change-specific lessons first.
- Run `memory review` and `memory compress` before promotion when the change trail gets noisy.
- Promote stable rules into the active host memory file only.
- Use `memory reflect` to capture a short post-gate note for the same host.
