---
description: Compare the current metrics snapshot against a prior reference
---

Use `metrics compare` to compare the current metrics snapshot against another snapshot path or the previous history entry.

Requirements:

- accept an explicit snapshot path when provided
- keep the comparison read-only
- surface deltas for files, lines, remotes, pcall coverage, and brain notes

Metrics compare input:
{{args}}
