---
description: Emit the delivery gate contract from the executable enforcement path
---

Use the active profile and current workspace artifacts to emit a valid `[DELIVERY GATE]`.

Requirements:

- validate the upgrade, brain, training, change, status, and memory artifacts
- call out missing files explicitly
- end with a clear `Blocked` or `Ready`

Delivery input:
{{args}}
