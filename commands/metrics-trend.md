---
description: Compare the current metrics snapshot against recent history
---

Use `metrics trend` to compare the latest metrics snapshot against the previous recorded run.

Requirements:

- report file and line deltas clearly
- include the current history length
- keep the output read-only

Metrics trend input:
{{args}}
